spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/waiwatts}
    username: ${DB_USER:waiwatts}
    password: ${DB_PASSWORD:waiwatts}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
