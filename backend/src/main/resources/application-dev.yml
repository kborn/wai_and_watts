spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5452/waiwatts}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
